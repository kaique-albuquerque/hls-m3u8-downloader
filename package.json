{
  "name": "video-downloader",
  "version": "1.0.0",
  "description": "Baixa videoaulas HLS (.m3u8) via FFmpeg — para URLs às quais você já tem acesso autorizado.",
  "type": "module",
  "main": "src/index.js",
  "scripts": {
    "download:curl": "node src/index.js --curl-impersonate",
    "test": "node test-curl-e2e.mjs",
    "ffmpeg:install": "node scripts/install-ffmpeg.mjs",
    "postinstall": "node scripts/install-ffmpeg.mjs"
  },
  "engines": {
    "node": ">=20"
  },
  "keywords": [
    "hls",
    "m3u8",
    "ffmpeg",
    "download",
    "video"
  ],
  "license": "MIT",
  "devDependencies": {
    "ntl": "^5.1.0"
  }
}
